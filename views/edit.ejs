<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit Memo</title>
    <style>
      label {
        display: block;
      }
    </style>
    <link rel="stylesheet" href="/public/style.css">
  </head>
  <body>
    <h1 class="logo">note</h1>
    <h1>Edit Memo</h1>
    <form action="/update/<%= memo._id %>" method="POST">
      <div class="title-box">
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" value="<%= memo.title %>" maxlength="15">
      </div>
      <div class="content-box">
        <label for="content">Content:</label>
        <textarea id="content" name="content"><%= memo.content %></textarea>
      </div>
      <div>
        <button type="submit" class="save-btn">Save</button>
        <!-- <button type="submit" class="delete-btn" data-id="<%= memo._id %>">Delete</button> -->
        <form action="/delete/<%= memo._id %>?_method=DELETE" method="POST" class="delete-btn">
          <button type="submit">Delete</button>
        </form>
      </div>
      <a href="/" class="cancel-btn">Cancel</a>
    </form>

    <script src="/public/app.js"></script>
  </body>
</html>


